local Commune = {
    Server = require(script.Server);
    Client = require(script.Client);
    ServerCommune = require(script.Server.ServerCommune);
    ClientCommune = require(script.Client.ClientCommune);
}

return Commune